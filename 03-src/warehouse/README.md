# Warehouse

Microsoft Fabric Warehouse DDL scripts and schema definitions.

## Structure (to be created during Phase 5)

- `ddl/` — CREATE TABLE statements for fact and dimension tables
  - `spi_fact_indicators.sql`
  - `spi_dim_indicator.sql`
  - `spi_dim_region.sql`
  - `spi_dim_calendar.sql`
  - `spi_dim_measure.sql`
  - `spi_dim_source.sql`
- `data/` — Reference data inserts (dim_region, dim_calendar, dim_measure, dim_source)

## Status

🚧 Empty — populated during Phase 5 development.
