# Data Pipelines

Microsoft Fabric Data Pipelines (JSON definitions, exported from Fabric workspace).

## Structure (to be created during Phase 5)

- `spi_pl_master.json` — master orchestrator (Bronze → Silver → Gold)
- `spi_pl_bronze.json` — ingest all 5 sources to Bronze
- `spi_pl_silver.json` — Bronze → Silver transformations
- `spi_pl_gold.json` — Silver → Gold loading

## Status

🚧 Empty — populated during Phase 5 development.
