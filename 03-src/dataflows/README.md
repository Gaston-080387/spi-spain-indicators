# Dataflows Gen2

Microsoft Fabric Dataflows Gen2 used for low-complexity transformations (Sources 1-2: IPC, IPI).

## Structure (to be created during Phase 5)

- `spi_df_silver_ipc/` — IPC Bronze → Silver cleansing
- `spi_df_silver_ipi/` — IPI Bronze → Silver cleansing
- `spi_df_gold_ipc/` — IPC Silver → Warehouse loading
- `spi_df_gold_ipi/` — IPI Silver → Warehouse loading
- `spi_df_gold_dimensions/` — Dimension table generation

## Status

🚧 Empty — populated during Phase 5 development.
